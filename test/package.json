{
  "name": "datastar-patterns-e2e",
  "private": true,
  "version": "0.0.1",
  "description": "Playwright end-to-end tests (headless verification for worker-coexistence fixes).",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
